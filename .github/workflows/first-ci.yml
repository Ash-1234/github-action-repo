name: first-ci

on:
  push:

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout-to-repo
        uses: actions/checkout@v4
        with:
          ref: main

      - name: list of files
        run: ls -la